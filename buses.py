import subprocess as sp
import os
var = 1
buses = 1 # initialization some raw value
stops = [] # creating a list

buses = input("no of buses stops")
print(buses)
stops = input("inout the bus stop number")
while var < buses:
        var = var + 1
        b = input("input the bus stop number sequentially")
        stops.append(b)
        print(stops)
        for x in stops:
          print(x)
          if x = 1

